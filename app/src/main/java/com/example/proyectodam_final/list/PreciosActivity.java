package com.example.proyectodam_final.list;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import androidx.recyclerview.widget.LinearLayoutManager;
import androidx.recyclerview.widget.RecyclerView;
import com.example.proyectodam_final.R;

import java.util.ArrayList;

public class PreciosActivity extends AppCompatActivity {

    //private PriceAdapter preciosAdapter;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_precios);

        /*RecyclerView recyclerViewPrecios = findViewById(R.id.recycler_view_precios);
        preciosAdapter = new PriceAdapter(this);
        recyclerViewPrecios.setAdapter(preciosAdapter);*/

    }
}
